package br.com.lotto.dao;

import java.util.List;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import org.springframework.stereotype.Repository;

import br.com.lotto.entity.Frequencia;
import br.com.lotto.entity.Megasena;

@Repository
public interface MegaSenaRepository extends JpaRepository<Megasena,Integer>{

	@Query(value = "select count(u.numeroidnumero) from megasenanumero u group by u.numeroidnumero order by u.numeroidnumero desc",nativeQuery=true)
	public Object[] getFrequancia();
}
